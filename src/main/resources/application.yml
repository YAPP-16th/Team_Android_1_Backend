server:
  port: 20000

  tomcat:
    accesslog:
      enabled: true
      pattern: common
      encoding: utf-8

spring:
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    open-in-view: false
    show_sql : true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true


  datasource:
    platform: mariadb
    url: jdbc:mariadb:failover://localhost:3307/erooja_test?autoReconnect=true
    username: root
    password: 1234
    driverClassName: org.mariadb.jdbc.Driver

  data:
    rest:
      basePath=/api

jwt:
  secret: jwtLocalSecret

KAKAO_ADMIN_KEY: 611a38980c02a420fac02e66330bc526
KAKAO_REST_API_KEY: 56711aad026298f90acfba38429b10fa
