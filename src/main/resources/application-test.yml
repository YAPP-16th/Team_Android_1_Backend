logging:
  level:
    root: warn
    com.eroom: info

spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

  datasource:
    platform: org.hibernate.dialect.H2Dialect
    url: jdbc:h2:mem:erooja-test
    username: sa
    password:
    driverClassName: org.h2.Driver